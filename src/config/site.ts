/**
 * TODO: replace `url` with the real production domain before launch — this
 * placeholder feeds canonical URLs and JSON-LD structured data.
 */
export const siteConfig = {
  name: "Life Helpers Initiative",
  shortName: "LHI",
  description:
    "Life Helpers Initiative (LHI) delivers rapid crisis response, community programs, and audited impact reporting.",
  url: process.env.NEXT_PUBLIC_SITE_URL ?? "https://example.org",
};
